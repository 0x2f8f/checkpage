<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{{ path('homepage') }}">{{ project_title }}</a>
    </div>
    <!-- Top Menu Items -->
    <ul class="nav navbar-right top-nav">
        {% if(app.user) %}
            {#{% include 'ItBlasterMainBundle:Layout:top_messages.html.twig' with {'foo': 'bar'} %}#} {# сообщения в шапке #}
            {#{% include 'ItBlasterMainBundle:Layout:top_notifications.html.twig' with {'foo': 'bar'} %}#} {# уведомления в шапке #}
        {% endif %}

        <li class="dropdown">
            {% if(app.user) %}
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i> {{ app.user.email }} <b class="caret"></b></a>
                <ul class="dropdown-menu">
                    {#
                    <li>
                        <a href="#"><i class="fa fa-fw fa-user"></i> Profile</a>
                    </li>
                    <li>
                        <a href="#"><i class="fa fa-fw fa-envelope"></i> Inbox</a>
                    </li>
                    <li>
                        <a href="#"><i class="fa fa-fw fa-gear"></i> Settings</a>
                    </li>
                    <li class="divider"></li>
                    #}
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}"><i class="fa fa-fw fa-power-off"></i> Выход</a>
                    </li>
                </ul>
            {% else %}
                <a href="{{ path('fos_user_security_login') }}">Войти</span><span class="icon"></a>
            {% endif %}

        </li>
    </ul>

    {% if(app.user) %}
        <!-- Sidebar Menu Items - These collapse to the responsive navigation menu on small screens -->
        {{ render(controller("ItBlasterMainBundle:Layout:menuLeft", {'request':app.request})) }} {# Левое меню #}
        <!-- /.navbar-collapse -->
    {% endif %}
</nav>